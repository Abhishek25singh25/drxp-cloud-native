apiVersion: v1
kind: PersistentVolumeClaim

metadata:
  name: mongodb-pvc
  namespace: drxp

spec:

  storageClassName: ""
  accessModes:
    - ReadWriteOnce

  resources:
    requests:
      storage: 1Gi